import { ActivePointerPipe } from './active-pointer.pipe';

describe('ActivePointerPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivePointerPipe();
    expect(pipe).toBeTruthy();
  });
});
